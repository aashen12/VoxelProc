# For Jonathan: Use roxygen to document the code

dataDrivenClusters <- function(voxel_df, n_pca = 20, n_umap = 2, n_clust = 2,
                               region = NULL) {
  # insert code here
  # Suggestions:
  #  use the "irlba" R package for PCA/SVD
  #  use the "umap" package for umap
  # Ensure the output is in list format. Data frames should be tibbles.
}
